# dotfiles
My dotfiles.

# Installing
On a fresh Mac run:
```shell
xcode-select --install
git clone https://github.com/kyle-fredrickson/dotfiles.git ~/.dotfiles && cd ~/.dotfiles
/bin/bash setup.sh
```
